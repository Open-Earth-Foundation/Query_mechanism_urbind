interface:
  display_name: "Expect Browser Testing"
  short_description: "Run Expect browser QA for this repo"
  default_prompt: "Use expect-cli to validate Query Mechanism Urbind in a real browser, focusing on the changed user flows and reporting pass/fail with repro steps."
