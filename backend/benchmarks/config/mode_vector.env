# Vector-store benchmark mode.
VECTOR_STORE_ENABLED=true

# Use the default Chroma persistence and collection from .env/llm_config unless
# explicitly overridden elsewhere. Benchmark is runtime-only against existing index.
