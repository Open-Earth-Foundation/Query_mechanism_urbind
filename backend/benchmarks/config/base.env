# Shared benchmark settings used by both modes.
# This file is loaded first, then mode-specific files override keys.

# Benchmark default: SQL disabled for retrieval-focused comparisons.
ENABLE_SQL=false

# Chroma paths (same as local default; used when mode_vector.env is active).
CHROMA_PERSIST_PATH=.chroma

# Optional: lower chatter unless debugging benchmark internals.
LOG_LEVEL=INFO
